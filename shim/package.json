{
  "name": "@yuki-is-taka/uemcp",
  "version": "0.2.1",
  "description": "Auto-starting stdio MCP server that bridges Claude Code to Unreal Engine editors via UE's Python Remote Execution. Launcher per session; singleton HTTP daemon fans in to share the UE connection.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "uemcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "keywords": [
    "unreal",
    "unreal-engine",
    "mcp",
    "modelcontextprotocol",
    "claude-code",
    "uemcp"
  ],
  "author": "yuki-is-taka",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuki-is-taka/uemcp-dev.git",
    "directory": "shim"
  },
  "homepage": "https://github.com/yuki-is-taka/uemcp-dev#readme",
  "bugs": {
    "url": "https://github.com/yuki-is-taka/uemcp-dev/issues"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "unreal-remote-execution": "^1.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.8.0"
  }
}
